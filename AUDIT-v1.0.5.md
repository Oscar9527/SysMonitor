# SysMonitor v1.0.5 全面代码审计

审计日期：2026-08-22

审计对象：GitHub tag `v1.0.5`，commit `4272e646e1b61eef8e2799514d4d017d700c53ee`

工作分支：`codex/audit-v1.0.5`

## 1. 输入与可追溯性

| 输入 | SHA-256 / 标识 |
| --- | --- |
| 上游源码 | `4272e646e1b61eef8e2799514d4d017d700c53ee` |
| 外部《开发文档.md》 | `A01BF71360AB35B1DFA3C7ED2B8D62E9B7A124A80E0F4FDA5560A42E3A488E8B` |
| 外部《开发进度.md》 | `89D02F422E75519EC41DD2776944E17C249379EF2B6B7E89A5335F45842431A2` |

两份外部文档只读审查，未作修改。文档中的测试总数存在 332 与 350 的冲突；仓库内旧 `VALIDATION.md` 又混入 1.5.0/322 次测试结果，均不能作为 v1.0.5 当前验证证据。本报告只采用本分支重新执行的结果。

## 2. 审计结论

未发现可稳定复现的 P0。已修复 9 类 P1 和 5 类 P2 问题，重点是误杀无关进程、采集器残留、设置溢出与锁回调、伪内存优化、UI 队列积压、运行时/版本错配、外部配置副作用顺序，以及启动失败后的不可重入状态。

### P1：已修复

1. **按名称误杀无关进程**：应用退出/启动清理会结束同一会话内不属于本实例的 `PresentMon*` 或 `SysMonitor*`。现已删除全局名称扫描；只处理持有句柄的自有采集器。
2. **PresentMon 孤儿进程**：正常终止和两次有界等待失败后，原实现可能直接遗留采集器。现增加对已记录自有进程的最终终止与等待。
3. **设置修订号溢出**：持久化 `long.MaxValue` 会使公共保存/补丁接口抛出；把最大值归一为 0 又会与刚从 `MaxValue - 1` 提交的实例产生分歧。现保留所有非负持久化修订号，并在最大值的下一次提交安全回绕到 1。
4. **设置回调死锁/覆盖**：用户补丁回调在全局与实例锁内运行，可与另一实例互锁。现先取快照、锁外执行回调，再在同一文件修订号未变化时提交；冲突明确失败且不重放回调。
5. **周期性强制 GC 与工作集裁剪**：Gen2 全量收集和 `SetProcessWorkingSetSize(-1,-1)` 会制造暂停与后续缺页，仅让任务管理器数字短暂下降。相关服务与调用已删除。
6. **50 MiB GC 堆硬限制**：有效负载也可能触发 OOM。运行时模板已删除，使用 .NET 默认 GC 策略。
7. **UI 快照无界排队**：每个监控快照都创建 Dispatcher 闭包，UI 忙时可积压旧数据与对象。现通过调度器合并，只渲染最新快照；历史缓冲仍在生产侧有界记录。
8. **目标框架、启动器检查与版本错配**：核心原为 net7，启动器只检查 .NET 8，构建文件又硬编码 1.5.0。现统一 `net8.0-windows`，版本从项目文件生成，启动器检查与核心要求一致。
9. **RTSS 外部副作用过早**：RTSS 配置在陈旧状态检查和设置落盘前变更，后续失败会留下半完成状态。现先完成本地校验/持久化，RTSS 失败时回滚本地设置并记录结果。

### P2：已修复

1. GPU 协调器启动失败时，监控服务现在回收已启动组件、取消令牌和状态，并可再次启动。
2. CPU `ReaderReady` 在释放信号量附近可能竞态；现在先退订，事件处理器容忍已释放/已满状态。
3. 详细 HUD 隐藏后仍读取 CPU/内存频率；现在只有“用户要求显示且当前为详细预设”才启用。
4. P/Invoke 未限定 DLL 搜索路径；程序集现在默认仅从 System32 解析系统 DLL。
5. 单文件压缩曾对框架依赖发布无条件启用；现在只对自包含单文件启用。

## 3. 性能与内存判断

- 删除的强制 GC、工作集裁剪和 50 MiB 堆上限不是可靠的内存优化，会把成本转化为 GC 暂停、缺页和 OOM 风险。
- UI 合并从“每次采样一个 Dispatcher 工作项”变为“最多一个待执行工作项 + 一个最新快照引用”，消除了 UI 阻塞期间由采样速率驱动的队列增长。
- 隐藏详细 HUD 时不再保留 CPU/内存频率轮询，减少无可见收益的 WMI/传感器工作。
- 没有把 19–30 MB、0.1% CPU、0.1 W 精度或“性能 100% 不变”作为结论。这些数字缺少可复现环境、采样周期与原始数据。WPF 工作集还包含映射程序集、字体与图形资源，不能等同于托管堆或泄漏。

## 4. 验证证据

打包主机为 Windows 11 企业版 10.0.22631 x64，Windows PowerShell
5.1.22621.4249；工具链为 .NET SDK 8.0.424、.NET/Windows Desktop Runtime
8.0.30 x64、MSBuild 17.11.48.46605、C# 编译器
4.11.0-3.25569.22。启动器使用 64 位 .NET Framework
`v4.0.30319` 引用目录（注册表 Version `4.8.09032`、Release `533320`）。
完整环境和发布参数见 `SysMonitor/VALIDATION.md`。

| 检查 | 结果 |
| --- | --- |
| Release 干净重建 | 0 警告、0 错误 |
| 自动化测试 | 338 通过、0 失败、0 跳过 |
| NuGet 漏洞审计 | 当前源未报告已知漏洞 |
| v1.0.6 Light 单文件 | 5,520,384 字节；SHA-256 `320D947A22A65472902BE84940EF74C4663E8BBB14750CD29274ADC469134F33`；1.0.6.0；缺少 .NET 时提供微软官方 x64 Desktop Runtime 直达下载和 Standalone 提示 |
| v1.0.6 Standalone 单文件 | 73,551,420 字节；SHA-256 `2051710DC621D54BC12EA159E5C55F712DB15FD40D10066CC43724150AEFF7EA`；1.0.6.0；内置 .NET，无需另装运行时 |

本次验证没有启动 GUI，因此没有修改任务栏、托盘、开机启动、RTSS 配置或用户设置。

## 5. 仍保留的风险与优先级

### P2：发布前建议真机处理或验证

- **PresentMon PID 复用窗口**：以 PID 重新打开进程时理论上可能命中复用后的其他进程。彻底解决需要贯穿助手协议传递创建时间或可验证句柄；当前改动未扩大身份判断。
- **Job Object 降级路径**：系统或嵌套 Job 限制可能使绑定失败，代码会继续运行。文档不得承诺所有子进程“必然 0 ms 清理”。
- **RTSS 失败后的双重持久化故障**：RTSS 应用失败时会尝试回滚本地设置；如果回滚写盘也同时失败，磁盘可能暂留已应用的 HUD 设置。代码会记录 `settingsRollback=false`，真机故障注入应覆盖此路径。
- **窗口线程退出超时**：任务栏区域监控线程的 STA 退出依赖有界 Join；需要在 Explorer 重启、注销和异常窗口环境中压力测试。
- **硬件热路径分配**：CPU 温度树、GPU 协调和 HUD 字符串仍有可优化分配，但应先用真实硬件和采样器定位，不做会影响传感器正确性的盲目缓存。

### P3：观察后再改

- 动态诊断键集合理论上可增长；若长时间测试显示键来自无界外部输入，再增加容量与淘汰策略。
- 主题目录校验仍存在常见 TOCTOU/重解析点边界；当前属于本地同权限攻击面，若未来接收不可信主题包再做句柄级验证。
- `LibreHardwareMonitorLib 0.9.4` 有更新版本，但仓库已有 Ryzen 温度回归记录；在硬件矩阵通过前不盲目升级。其他包也应按兼容性测试而非仅按“最新”升级。

## 6. 发布门槛

1. 在 NVIDIA、AMD、Intel GPU 与至少两类 CPU 平台运行传感器矩阵。
2. 采集 2–24 小时的工作集、私有内存、GC 暂停、线程和句柄趋势。
3. 覆盖 Windows 10/11、多显示器、横/竖/自动隐藏任务栏和 100%–250% DPI。
4. 验证 DirectX/Vulkan/OpenGL、权限拒绝、ETW 冲突和目标退出；旧 DirectDraw 无事件必须显示无数据而不是伪造 FPS。
5. RTSS 路径会写入 Hook 配置并可能启用注入，必须明确标为可选且不能宣称对反作弊“100% 安全”。

完成这些真机门槛前，可以发布为审计候选构建，但不应把硬件精度、长期内存、全游戏兼容或反作弊兼容描述为已证明事实。
