<Window x:Class="SysMonitor.UI.GameOverlaySettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="HUD 设置" Width="490" Height="480"
        WindowStartupLocation="CenterScreen" ResizeMode="CanMinimize">
  <Grid Margin="20">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <TextBlock Text="显示项目与排列顺序" FontSize="16" FontWeight="SemiBold"/>
    <Grid Grid.Row="1" Margin="0,12,0,16">
      <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="92"/></Grid.ColumnDefinitions>
      <ListBox x:Name="MetricList" MinHeight="210">
        <ListBox.ItemTemplate><DataTemplate><CheckBox Content="{Binding Name}" IsChecked="{Binding Enabled, Mode=TwoWay}" Padding="8,6"/></DataTemplate></ListBox.ItemTemplate>
      </ListBox>
      <StackPanel Grid.Column="1" Margin="12,0,0,0">
        <Button Content="上移" Height="34" Margin="0,0,0,8" Click="MoveUp_Click"/>
        <Button Content="下移" Height="34" Click="MoveDown_Click"/>
        <TextBlock Margin="0,16,0,0" Foreground="Gray" FontSize="11" TextWrapping="Wrap" Text="选中项目后调整顺序；取消勾选即可隐藏。"/>
      </StackPanel>
    </Grid>
    <Grid Grid.Row="2" Margin="0,0,0,16">
      <Grid.ColumnDefinitions><ColumnDefinition Width="110"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
      <TextBlock Text="采样频率" VerticalAlignment="Center"/>
      <ComboBox x:Name="SamplingBox" Grid.Column="1" Height="34" SelectedValuePath="Tag">
        <ComboBoxItem Content="低（2 秒，最省资源）" Tag="low"/>
        <ComboBoxItem Content="标准（1 秒，推荐）" Tag="standard"/>
        <ComboBoxItem Content="高（0.5 秒，响应更快）" Tag="high"/>
      </ComboBox>
    </Grid>
    <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
      <Button Width="92" Margin="0,0,8,0" Content="应用" Click="Apply_Click"/>
      <Button Width="92" Content="取消" Click="Cancel_Click"/>
    </StackPanel>
  </Grid>
</Window>
