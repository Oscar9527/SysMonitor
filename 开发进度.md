# SysMonitor 开发进度与功能里程碑

> **当前版本**：v1.0.5  
> **测试状态**：332/332 单元测试全部通过（通过率 100%）  
> **代码仓库**：`https://github.com/Oscar9527/SysMonitor`

---

## 📌 版本迭代演进历史

### 🚀 v1.0.5 (当前最新版本)
- [x] **功耗监测扩展**：新增 CPU 与 GPU 实时功耗检测（W），精确到 0.1W，任务栏与 HUD 支持独立勾选开关。
- [x] **矩阵式监控项自定义配置**：
  - 任务栏与游戏浮层全面重构为多维度矩阵排版（使用率、温度、功耗、频率、容量）；
  - 支持指标上下拖拽/调序；
  - 修复勾选显存/内存容量时占用率百分比被覆盖的问题，重构为规范的上下双层排版。
- [x] **单文件无损压缩瘦身**：
  - 剥离调试符号（PDB），开启微软原生单文件流式 Deflate 压缩；
  - 独立版体积由 163MB 瘦身至 **69.7MB**（性能与功能 0 损失）；
  - 轻量版保持 **4.91MB**。
- [x] **高 DPI 全局深度自适应**：
  - 应用程序清单原生开启 `PerMonitorV2`；
  - 任务栏监控条与设置界面在 100%~200% 多屏缩放下自适应重绘，设置界面动态贴合工作区。
- [x] **全代码审计与开发文档**：编写完整的代码审查审计文档（`开发文档.md`），完善全模块详细注释。

---

### 📦 v1.0.4
- [x] **窗口化游戏边缘贴靠与前台焦点同步**：HUD 支持自动吸附游戏窗口，游戏最小化/切后台时 HUD 自动隐藏。
- [x] **CPU 快速采样优化**：优化启动初期的硬件传感器探测阈值。

---

### 📦 v1.0.3
- [x] **深色/浅色主题自适应**：自动读取注册表 `SystemUsesLightTheme`，暗色模式深度优化。
- [x] **字体渲染调优**：修复规范简体中文字形在部分系统下的显示异常。

---

### 📦 v1.0.2
- [x] **鼠标多向自由缩放**：详情面板支持拖动边角任意缩放并持久化记忆尺寸。
- [x] **16px 纯净圆角与无黑边渲染**：重构 WPF 渲染层，解决透明通道黑边问题。
- [x] **多显示器副屏支持**：支持跨屏幕拖拽与不同 DPI 副屏坐标记忆。

---

### 📦 v1.0.1
- [x] **首发正式版**：任务栏嵌入式监控条、MSI Afterburner 经典配色 HUD、老游戏 DirectDraw 兼容、19MB 超低内存优化。

---

## 📊 核心模块开发与审查状态

| 模块名称 | 对应核心源文件 | 完成度 | 状态说明 |
| :--- | :--- | :---: | :--- |
| **中央调度中枢** | `Services/MonitorService.cs` | 100% | 稳定，无锁快照流水线，支持后台多线程安全轮询 |
| **CPU 监测引擎** | `Services/CpuTemperatureReader.cs` | 100% | 稳定，支持 Intel RAPL 功耗、AMD SMU 功耗与结温读取 |
| **GPU 监测引擎** | `Services/GpuTelemetryCoordinator.cs` | 100% | 稳定，支持 NVIDIA NVML、AMD/Intel LHM 核心频/显存频/功耗 |
| **游戏帧率引擎** | `Services/PresentMonFrameRateProvider.cs` | 100% | 稳定，PresentMon 2.5.1 ETW 零侵入 + RTSS 共享内存双通道 |
| **任务栏嵌入条** | `UI/BandWindow.xaml.cs` | 100% | 稳定，多屏 PerMonitorV2 DPI 自适应，上下双层排版 |
| **游戏浮层 HUD** | `UI/GameOverlayWindow.xaml.cs` | 100% | 稳定，穿透点击，自动吸附前台窗口，MSI 风格纯色方案 |
| **外观与矩阵配置** | `UI/AppearanceSettingsWindow.xaml.cs` | 100% | 稳定，九宫格矩阵勾选，多语言与主题热切换 |
| **内存与单文件优化** | `Services/MemoryOptimizer.cs` | 100% | 稳定，工作集修剪（19MB~30MB），流式压缩打包（69.7MB） |

---

## 🧪 测试与质量指标

- **单元测试覆盖**：332 个自动化测试，涵盖数据采集模型、布局算法、本地化、序列化、并发安全等，运行结果：`Passed: 332, Failed: 0, Skipped: 0`。
- **内存占用指标**：常驻物理内存稳定在 19MB ~ 30MB，CPU 占用稳定在 < 0.1%。
- **兼容性验证**：Windows 10 21H2+、Windows 11 22H2/23H2/24H2（x64 架构）。
