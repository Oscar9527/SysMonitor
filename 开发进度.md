# SysMonitor 开发进度与版本说明

本文件随源代码交付，记录从下载原始 SysMonitor 项目开始的改造过程。日期以本地开发记录为准；版本号用于交付识别，不代表上游 GitHub 发布版本。

## 0. 原始项目导入

- 来源：`https://github.com/Oscar9527/SysMonitor`
- 技术栈：WPF / .NET 8 / Windows。
- 改造目标：不依赖 MSI Afterburner 或 RTSS，提供低占用、可配置的游戏性能 HUD。

## 1. 独立硬件监控

- CPU 使用率采用与任务管理器一致的 PDH Processor Utility 数据。
- CPU 温度使用 LibreHardwareMonitor 的独立读取路径；必要时使用无界面的管理员助手。未读到传感器时显示空值，不伪造温度。
- NVIDIA GPU 使用 `nvidia-smi` 独立读取，并以 Windows Job Object 清理子进程，避免残留。
- 网络流量改为默认路由实际接口采样，避免 VPN、虚拟网卡或多适配器重复累计。
- 内存详细 HUD 增加内存配置频率（WMI `ConfiguredClockSpeed`，读取失败时回退 `Speed`）；这是配置频率，不虚称为实时内存时钟。

## 2. 游戏帧率采集

- 移除对 MSI / RTSS 的依赖，内置 PresentMon 2.5.1 ETW 采集器。
- 托盘菜单可手动选择任意窗口作为目标，不依赖“游戏识别”。
- PresentMon 通过无窗口的管理员助手运行，主程序不弹出 CMD。
- 采集器进程由管理员助手自己的 Job Object 托管；助手、管道或应用异常退出时会清理采集器。
- 新启动采集前会清理仅位于 SysMonitor 自己运行目录的旧 PresentMon 进程，避免旧版残留占用 ETW。
- ETW 会话冲突、资源不足（1450）、权限不足、无帧事件等状态分开诊断；HUD 使用中文短提示，不显示英文 `unavailable`。

### 帧率数据边界

FPS 只在目标程序确实产生可由 Windows/PresentMon 观察到的呈现事件时显示。未启动目标、目标没有绘制帧、或采用旧式 DirectDraw/特殊渲染路径时，不会伪造 FPS；HUD 会显示“未选择目标”“正在采集”“未捕获到帧”或“采集失败”等真实状态。

## 3. HUD 与交互

- HUD 支持紧凑、RivaTuner 风格、详细三种布局。
- 支持监控项目开关、项目上下排序、低/标准/高三档采样频率。
- 支持字体下拉选择、字号、描边、阴影、GPU/CPU/FPS/RAM/网络的独立颜色，以及 Windows 原生色轮。
- 内置四套外观：经典霓虹、冰川蓝、落日暖色、简洁白。
- HUD 定位改为以所选窗口的客户区为基准，适用于窗口化游戏。
- HUD 保持独立进程外窗口，不向游戏注入 DLL，也不使用跨进程 `SetParent`。窗口移动/缩放时通过 Windows 位置事件进入约 16 ms 的同步跟随，结束时再做一次最终对齐；平时回到低频恢复检查。
- HUD 已取消黑色圆角背景，实际窗口和设置预览均为全透明；可分别调整监控项文字颜色、描边强度、阴影清晰度和阴影偏移。
- 水平和垂直布局都严格按用户勾选项及排序显示，内存与网络不再被水平布局强制隐藏。
- 托盘右键菜单按当前显示器、DPI 和中英文文字自动测量宽高；超出工作区时限制尺寸并保留滚动访问，避免菜单或快捷键被裁掉。

## 4. 性能与稳定性

- 移除系统字体全量预热，降低启动内存。
- 仅在详细 HUD 模式读取 CPU 频率与内存频率。
- CPU 温度优先使用 CPU-only 传感器树，连续无数据后才扩大探测范围。
- 未使用强制工作集裁剪等会引起卡顿的伪优化。
- 最近测得冷启动工作集约 129 MB、私有内存约 52 MB（WPF/.NET 工作集包含字体、DirectWrite、图形与程序集映射，不能等同于可释放内存）。

## 5. 当前交付版本

- 核心程序集版本：`1.5.0.0`
- 便携启动器：`artifacts/SysMonitor.exe`
- 桌面副本：`%USERPROFILE%/Desktop/SysMonitor-新版.exe`
- 最近构建 SHA-256：`D190C9AB2153B99609D067DB631F10EE3B463A7B3214D4E6F52B3E82CC2954ED`
- 最近完整验证：322/322 通过；透明 HUD、文字描边阴影、水平内存显示、项目筛选排序及窗口跟随均有定向覆盖。

## 6. 源代码目录

- `SysMonitor/`：WPF 主程序、服务、模型、资源和内置第三方文件。
- `SysMonitor.Tests/`：单元测试与定向 UI/原生样式测试。
- `Launcher/`：便携启动器及构建脚本。
- `artifacts/`：最新生成的便携 EXE。
- `README.md`、`SysMonitor/README.md`、`SysMonitor/VALIDATION.md`：使用、架构和验证说明。

## 7. 已知限制与下一步

- CPU 功率、内存温度、部分 GPU 功率只能在本机硬件传感器真正提供时显示，不能生成虚假数据。
- 一些旧游戏的 DirectDraw 或特殊渲染路径不会产生 PresentMon 可读取的呈现事件；要获得真实 FPS，需要针对该渲染 API 开发专门采集后端或进行高风险注入，不应以屏幕刷新率冒充游戏 FPS。
- 出于反作弊兼容与稳定性考虑，不承诺真正“注入式嵌入”游戏。当前方案是系统事件驱动的进程外视觉贴合；普通窗口拖动可快速同步，但独占全屏、受保护窗口或系统阻止读取时仍会安全退回低频定位。
