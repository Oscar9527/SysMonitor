# SysMonitor v1.0.6 审计与发布进度

最后更新：2026-08-22

交付版本：`v1.0.6`

基线：`v1.0.5` / `4272e646e1b61eef8e2799514d4d017d700c53ee`

## 已完成（按优先级）

### P0

- 未发现可稳定复现的 P0（立即崩溃、任意代码执行或必然数据破坏）问题。

### P1

- 删除按进程名终止全部 `SysMonitor*` / `PresentMon*` 的逻辑，只清理本实例明确拥有的采集器。
- PresentMon 正常终止与等待均失败时，最终强制终止已记录的自有进程并进行有界等待。
- 修复设置修订号达到 `long.MaxValue` 时保存或补丁调用溢出，并保证最大值前后跨实例观察一致。
- 设置回调移出共享锁，加入按路径串行门与冲突检测，避免跨实例回调死锁及静默覆盖。
- 删除启动、每 60 秒和隐藏窗口时的强制 Gen2 GC/工作集裁剪；移除 50 MiB GC 硬上限。
- UI 快照改为只保留最新值，避免 Dispatcher 忙碌时积压无界闭包。
- 统一为 `.NET 8`，启动器、核心和生成版本均读取项目的 `1.0.6` 版本源。
- 将 RTSS 外部配置变更移到本地校验与设置持久化之后；失败时尝试回滚本地设置。

### P2

- 监控服务启动中途失败时清理 CPU/GPU/温度/网络状态并允许再次启动。
- 释放 CPU 读取器前先退订事件，并容忍并发事件与信号量已释放竞态。
- HUD 隐藏时停止详细 CPU/内存频率读取。
- 为 P/Invoke 增加系统目录 DLL 搜索限制。
- 修复框架依赖发布误启单文件压缩导致的 `NETSDK1176`。

## 验证状态

- 打包环境：Windows 11 企业版 Build 22631 x64；.NET SDK 8.0.424；Windows Desktop Runtime 8.0.30 x64；MSBuild 17.11.48.46605；C# 4.11.0-3.25569.22。
- Release 干净重建：0 警告、0 错误。
- 自动化测试：338/338 通过。
- Light 单文件：5,520,384 字节，版本 1.0.6.0；缺少 .NET 时可一键进入微软官方 x64 Desktop Runtime 下载。
- Standalone 单文件：73,551,420 字节，版本 1.0.6.0；内置 .NET，无需另装运行时。
- NuGet 当前源：未发现已知漏洞。

## 下一步真机验证

- NVIDIA/AMD/Intel 与不同 CPU 平台的传感器可用性和数值合理性。
- 2–24 小时工作集、私有内存、GC 暂停与句柄/线程趋势，不以单点工作集作为内存泄漏结论。
- Windows 10/11、横/竖任务栏、多显示器、100%–250% DPI 与自动隐藏任务栏。
- DirectX/Vulkan/OpenGL 的 PresentMon 采集、RTSS 可选路径及权限拒绝场景。
- 常见反作弊环境兼容性；不得宣称“100% 安全”或“零风险”。

完整问题台账、证据和未解决风险见 `AUDIT-v1.0.5.md`。
