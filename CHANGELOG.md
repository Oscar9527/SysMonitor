# Changelog

## [1.2.16] - 2026-08-11

- 详情面板的存储区域改为显示所有可用固定磁盘，并展示每个磁盘的已用容量、总容量和使用率。
- 任务栏 Band 仍只显示系统盘，新增 CPU、内存、GPU、下载、上传和系统盘的独立显示开关。
- 隐藏项目后会重新计算 Band 的实际宽度和分隔线，不保留空白槽位；全部关闭时可从托盘恢复设置。
- 磁盘枚举改为 10 秒后台缓存并增加故障宽限，避免慢速或暂时不可用的磁盘影响 1 秒主采样循环。
- 增加旧设置迁移、动态布局、GPU 显示防抖和多磁盘故障场景测试。
- 修复便携打包复用旧 `artifacts\SysMonitor.exe` 时可能把新版启动器与旧核心组合在一起的问题。

## [1.2.15] - 2026-08-11

- 修复自动隐藏任务栏动画期间使用旧屏幕坐标反复校正的问题；Band 现在保持父任务栏客户区相对坐标并随原生父窗口移动。
- 健康检查在窗口契约正常时不再调用 `SetWindowPos`，降低 Windows 10 任务栏应用指示线重绘和闪烁风险。
- 任务栏安全边界改为“向内立即收紧、向外连续确认”，当前 Band 仍在安全区内时保持物理像素位置，消除 1–2 px 边界波动造成的左右抖动。
- 新增“跟随系统 / 简体中文 / English”即时语言切换，设置会持久化，详情面板、外观窗口和托盘菜单无需重启即可更新。
- 详情面板和外观窗口统一为更现代的浅色分组卡片界面，并保留 Windows 窗口操作习惯与 Win10 字体回退。
- 增加任务栏几何、边界稳定、本地化资源和设置迁移测试；完整测试增至 58 项。

## [1.2.14] - 2026-08-10

- 新增 AMD、Intel GPU 采集，并保留 NVIDIA `nvidia-smi` 高精度主通道。
- 双显卡根据实际使用率自动选择，并增加切换防抖，避免核显/独显名称来回跳动。
- GPU 温度、使用率和显存支持“未知”状态，不再将驱动未公开的数据伪装为 0。
- GPU 核心温度只接受明确的核心传感器，不混用热点、显存结温或 VRM 温度。
- 升级到受支持的 .NET 8 Desktop Runtime，并选用已验证可兼顾三家 GPU 与 Ryzen 5000 温度的 LibreHardwareMonitorLib 0.9.4。

## [1.2.13] - 2026-08-10

- 新增任务栏项目间距设置，范围 0–18 px，并持久化到用户配置。
- Band 宽度根据项目间距动态计算，扩大安全区域内的左右移动范围。
- 任务栏边界识别优先使用真实按钮边界，忽略宽大的空任务列表容器。
- 启动新版时自动关闭旧版 SysMonitor runtime 核心，避免新版本被旧互斥锁拦截。
- 启动器对已释放核心执行 SHA-256 校验，内容过期时自动替换缓存。
- 外观设置标题显示当前核心版本，便于跨电脑确认实际运行版本。

## [1.2.12]

- 增加项目固定槽位、等宽数字和自适应 Band 宽度。
- 改善任务栏安全区域计算，减少位置范围被空容器压缩的问题。

## [1.2.11]

- 修复 Windows 10 任务栏兄弟窗口重绘干扰。
- 健康检查不再每秒强制重新定位 Band。
- 增加 `WS_CLIPSIBLINGS`，降低任务栏应用运行指示线闪烁。

## [1.2.9]

- 修复点击任务栏 Band 后闪烁、消失和重复创建窗口的问题。
- 保留同一个 Band HWND，并在 Explorer 重启后进行受控恢复。
